
spring:
  application:
    name: consumer-user #這個名子會記錄在註冊中心上

server:
  port: 8200

eureka:
  instance:
    prefer-ip-address: true #註冊服務時候的ip地此
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
